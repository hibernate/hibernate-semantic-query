// Rename the root project in Gradle terms.  This allows the
// "nicer" `hibernate-semantic-sqm` name to be used in as
// the git repo name, but the more concise `hibernate-sqm`
// name to be used for the artifact(s)
rootProject.name = 'hibernate-sqm'